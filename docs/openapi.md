# OpenAPI

Com a API rodando, acesse:

```text
http://localhost:3333/docs
```

O schema e gerado pelo NestJS Swagger a partir dos controllers e DTOs. Os frontends devem consumir esse contrato para gerar um client tipado em uma etapa futura de CI.
